export enum HttpStatusCode {
  OK = 200,
  CREATED = 201,
  SUCCESS = 202,
}

export enum RequestStatus {
  SUCCESS = "Success!",
  FAILED = "Failed!",
  FAILED_ = "Failed",
  IDLE = "Idle",
  LOADING = "Loading",
  UNABLE_TO_CONNECT = "UnableToConnect",
}
